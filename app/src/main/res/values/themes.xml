<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.ProyectoPDMD" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/generalText</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="android:toolbarStyle">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/generalText</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/white</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->

        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="editTextStyle" >
        <item name="android:textColor">@color/generalText</item>
        <item name="android:textColorHint">@color/iconColor</item>
        <item name="android:backgroundTint">@color/generalText</item>
        <item name="passwordToggleTint">@color/generalText</item>
    </style>

</resources>